# The workload runs under its own account with no token mounted. origod
# never calls the API server (spec 001, invariant 7), and a mounted token is
# a credential an attacker inside the container can use.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: origod
automountServiceAccountToken: false
