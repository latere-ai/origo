self-hosted-runner:
  # Declared so actionlint accepts the label if a workflow ever asks for the
  # organization's own runner. Every job in this repository runs on
  # ubuntu-latest, so a fork needs nothing set up.
  labels:
    - linux-vm
