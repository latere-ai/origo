self-hosted-runner:
  # Declared so actionlint accepts the label where a job asks for the
  # organization's own Linux runner. Every job defined in this repository
  # runs on ubuntu-latest, so a fork needs no runner set up.
  labels:
    - linux-vm
