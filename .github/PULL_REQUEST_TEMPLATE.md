## What this changes

<!-- What behaviour is different after this, for whoever uses or runs Origo. -->

## Why

<!-- The problem it solves. Link the issue or the spec if there is one. -->

## Checklist

- [ ] `make` passes locally.
- [ ] `make test-integration` passes, or this change does not touch the log.
- [ ] A bug fix carries a test that fails without it.
- [ ] A change to the contract, the log format, a configuration variable, or an
      event payload is reflected in its spec and in `docs/`.
- [ ] Generated pages are current (`make docs` leaves no diff).
